#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define Boost                         \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);                    \
    cout.tie(NULL)
int main()
{
    Boost;
    int n;
    cin >> n;
    cout << 25 << endl;
    /*  long long int r = 1, sum = 0, remind;

     while (n != 0)
     {
         r *= 5;
         --n;
     }

     if (r < 100)
     {
         cout << r;
     }
     else
     {
         remind = (r % 100);
         cout << remind;
     } */
    return 0;
}